# Copy this file to .env.local for local development and fill your keys.
# Do NOT commit your actual API keys to version control.

# FEC API key (https://api.open.fec.gov/)
NEXT_PUBLIC_FEC_API_KEY=

# Congress.gov API key (if used)
NEXT_PUBLIC_CONGRESS_API_KEY=

# Optional: override Congress.gov base url (rarely needed)
# NEXT_PUBLIC_CONGRESS_API_URL=https://api.congress.gov/v3

# Notes:
# - For local dev, create a file named .env.local with values from above.
# - For Vercel, add these keys in the Project > Settings > Environment Variables.
